'user strict';

import {Name}Ctrl from './{name}.ctrl';

describe('{Name}Ctrl', () => {
  let ctrl;

  beforeEach(() => {
    ctrl = new {Name}Ctrl();
  })

  // it('should ...', () => {
  //   expect(1 + 1).toEqual(2);
  // })
})
